<h1>Tasks</h1>
<%= link_to "新規登録", new_task_path %>
<table>
    <%  @tasks.each do |task| %>
        <tr>
            <td><%= task.title %></td>
            <td><%= task.content %></td>
            <td><%= link_to "編集", edit_task_path(task)%></td>
        </tr>
    <% end %>
</table>